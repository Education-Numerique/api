routing:
################################################################################
#
#   Activities
#
################################################################################


  - url: ^/:version/activities(/)?$
    vars:
        version: ([0-9]\.[0-9])
    params:
        service: graph.activities
        module: Activities
        action: create
    method: post

  - url: ^/:version/activities(/)?$
    vars:
        version: ([0-9]\.[0-9])
    params:
        service: graph.activities
        module: Activities
        action: list
    method: get

  - url: ^/:version/activities/:rid(/)?$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: fetch
    method: get

  - url: ^/:version/activities/:rid(/)?$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: delete
    method: delete

  - url: ^/:version/activities/:rid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: save
    method: post

  - url: ^/:version/activities/:rid/publish$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: publish
    method: post

  - url: ^/:version/activities/:rid/unpublish$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: unpublish
    method: post

  - url: ^/:version/activities/:rid/report$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: report
    method: post

  - url: ^/:version/activities/:rid/seen$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Activities
        action: seen
    method: post

################################################################################
#
#   Thumbnail
#
################################################################################


  - url: ^/:version/activities/:rid/thumbnail$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Thumbnail
        action: save
    method: post

  - url: ^/:version/activities/:rid/thumbnail$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Thumbnail
        action: delete
    method: delete

################################################################################
#
#   Media
#
################################################################################


  - url: ^/:version/activities/:rid/media$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Media
        action: create
    method: post

  - url: ^/:version/activities/:rid/media/:mid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        mid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Media
        action: fetch
    method: get

  - url: ^/:version/activities/:rid/media/:mid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        mid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Media
        action: update
    method: post

  - url: ^/:version/activities/:rid/media/:mid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        mid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Media
        action: delete
    method: delete

################################################################################
#
#   Attachments
#
################################################################################


  - url: ^/:version/activities/:rid/attachments$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Attachments
        action: create
    method: post

  - url: ^/:version/activities/:rid/attachments/:aid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        aid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Attachments
        action: fetch
    method: get

  - url: ^/:version/activities/:rid/attachments/:aid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        aid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Attachments
        action: update
    method: post

  - url: ^/:version/activities/:rid/attachments/:aid$
    vars:
        version: ([0-9]\.[0-9])
        rid: ([a-zA-Z0-9]{24})
        aid: ([a-zA-Z0-9]{24})
    params:
        service: graph.activities
        module: Attachments
        action: delete
    method: delete
